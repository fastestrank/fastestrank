## Summary

Briefly explain the intent of this PR and what problem it addresses.

## Checklist

- [ ] I have verified that changes to `.agents/skills/` have been synced using `npm run sync-skills`.
- [ ] `npm test` (`check-skills` and `check-repo`) passes locally.
- [ ] No private credentials, internal API keys, or self-hosted backend configurations were introduced.
- [ ] Documentation and manifest URLs match `https://app.fastestrank.com/mcp/` and official FastestRank domains.
